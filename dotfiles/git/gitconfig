[user]
   signingkey = 515C0ACE5D2939A2
   email = odedlaz@gmail.com
   name = Oded Lazar
[commit]
   gpgsign = false

[gpg]
   program = /usr/bin/gpg2

[pull]
   rebase = true

[alias]
	fco = !sh -c 'git fetch origin master && git checkout -b $1 origin/master' -
	rtl = !sh -c 'git fetch origin master && git rebase origin/master' -
	cor = !sh -c 'git fetch origin $1 && git checkout $1 && git reset --hard origin/$1' -
