#!/usr/bin/env fish
alias vi! (which vi)
alias vi (which nvim)
alias ls! (which ls)
alias ls exa

alias readme 'pandoc README.md | lynx -stdin'
alias pingg "ping google.com"
alias fuck='eval "sudo $history[1]"'
